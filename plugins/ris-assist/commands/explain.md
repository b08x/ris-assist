---
description: Explain a radiology IT concept, or start the onboarding path.
argument-hint: "[concept, or 'onboarding']"
allowed-tools: Read, Write
---

STUB — see `skills/explainer/SKILL.md` for the behavior this command invokes.

Arguments: $ARGUMENTS

If the site profile is missing or the needed field is unpopulated, say so in one
line and offer `/onboarding`. Continue with generic behavior, labelled as untuned.
