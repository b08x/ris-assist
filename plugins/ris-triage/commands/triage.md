---
description: Clarify and route a ticket — one discriminating question at a time.
argument-hint: "[ticket text or symptom]"
allowed-tools: Read, Write
---

STUB — see `skills/triage/SKILL.md` for the behavior this command invokes.

Arguments: $ARGUMENTS

If the site profile is missing or the needed field is unpopulated, say so in one
line and offer `/setup`. Continue with generic behavior, labelled as untuned.
